---
title: "Video Pages"
layout: "default"
isContentPage: true
---

<p>This is a list of published video page entries.</p>

<ul>
    <% for page in @getCollection("videoPages").toJSON(): %>
    <li class="<%= if page.id is @document.id then 'active' else 'inactive' %>">
		<a href="<%= page.url %>">
			<%= page.original.fields.name %>
		</a>
    </li>
    <% end %>
</ul>
