---
title: "Facilities"
layout: "default"
isContentPage: true
---

<p>This is a list of published facility entries.</p>

<ul>
    <% for page in @getCollection("facilities").toJSON(): %>
    <li class="<%= if page.id is @document.id then 'active' else 'inactive' %>">
		<a href="<%= page.url %>">
			<%= page.original.fields.description %>
		</a>
    </li>
    <% end %>
</ul>
