---
title: "Category Pages"
layout: "default"
isContentPage: true
---

<p>This is a list of published category page entries.</p>

<ul>
    <% for page in @getCollection("categoryPages").toJSON(): %>
    <li class="<%= if page.id is @document.id then 'active' else 'inactive' %>">
		<a href="<%= page.url %>">
			<%= page.original.fields.title %>
		</a>
    </li>
    <% end %>
</ul>
